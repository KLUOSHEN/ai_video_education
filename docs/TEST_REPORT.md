# 测试报告

测试命令：`npm.cmd test`

最近一次执行结果：5/5 通过（包含题目讲解视频分镜、搜索 → 异步生成 → 获取结果 → 服务端判题的 HTTP 集成链路）。

覆盖范围：

- 输入 Unicode/空白规范化、空输入和脚本/提示注入过滤。
- 选择、判断、填空三种教学题型及难度字段生成。
- 选择题和填空题服务端答案判定。

人工集成检查路径：启动服务后，在 `search.html` 提交主题，确认跳转到 `watch.html`；页面应显示生成进度，任务完成后显示课程封面、场景和三类题目。点击选项或提交填空后，页面调用 `POST /questions/{id}/grade` 并更新得分。
