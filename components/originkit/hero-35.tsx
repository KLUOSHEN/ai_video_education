"use client";

// 全局样式与 @theme 已由 app/globals.css 统一引入（含 hero-35 的字体与主题 token）
import { SectionHero } from "@/components/originkit/ui/hero-35/section-hero";

const Hero35 = () => <SectionHero />;

export default Hero35;
